#!/usr/bin/env ruby

require 'bundler/setup'
require 'reline'

Signal.trap('INT') do
  puts '^C'
  exit 0
end

puts 'This is echo program by Reline.'
while line = Reline.readline("> ")
  case line.chomp
  when 'exit', 'quit'
    exit 0
  when ''
    # NOOP
  else
    puts line
  end
end
puts
